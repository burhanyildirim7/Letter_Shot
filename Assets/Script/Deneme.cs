using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Deneme : MonoBehaviour
{
    /*List Kullanýmý Þehir ele-çýkart-count ölç
    List<string> Sehirler = new List<string>();
    [SerializeField] InputField SehirAdýText;
    [SerializeField] Text EkranCiktisiText;
    [SerializeField] Text SehirSayisiText;
    string EkranCiktisi;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        SehirSayisiText.text = Sehirler.Count.ToString();
    }

    public void Ekle() 
    {

        Sehirler.Add(SehirAdýText.text);
        EkranCiktisi = "";
        for (int i = 0; i < Sehirler.Count; i++)
        {
            EkranCiktisi = EkranCiktisi +"\n"+ Sehirler[i];
        }
        EkranCiktisiText.text = EkranCiktisi;
    }
    public void Sil()
    { 
        Sehirler.Remove(SehirAdýText.text);
        EkranCiktisi = "";
        for (int i = 0; i < Sehirler.Count; i++)
        {
            EkranCiktisi = EkranCiktisi + "\n" + Sehirler[i];
        }
        EkranCiktisiText.text = EkranCiktisi;
    }
    */


}
